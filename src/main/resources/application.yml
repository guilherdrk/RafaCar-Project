spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rafacar_db
    username: rafacar
    password: rafacar123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  application:
    name: rafacar-backend
  flyway:
    baseline-on-migrate: true
    baseline-version: 1

server:
  port: 8081

springdoc:
  swagger-ui:
    path: /swagger
